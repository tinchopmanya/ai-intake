# Legacy Backend Surface

This folder keeps references to deprecated backend flows that are no longer part of the active MVP API.

Active advisor/analysis/auth/onboarding/OCR implementation lives under `api/app/`.

Legacy modules are intentionally isolated to avoid accidental imports from runtime code.
